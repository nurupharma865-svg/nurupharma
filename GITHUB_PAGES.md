# Déploiement GitHub Pages — PharmaSys

Le fichier `index.html` à la racine est maintenant l'application PharmaSys complète.

Important :
- Ne pas ouvrir `app.html` : il n'est plus nécessaire.
- Le site doit être publié avec GitHub Actions.
- Le chemin GitHub Pages est `/nurupharma/`.
- Le fichier `.env` local n'est pas inclus dans cette archive.
- `schema.sql` et les migrations Supabase restent dans le projet.

URL attendue :
https://nurupharma865-svg.github.io/nurupharma/
